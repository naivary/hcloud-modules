# Kubernetes Cluster using Talos Linux

This module allows to create a Kubernetes Cluster using Talos Linux. The Cluster
is only accessable over a private network.

## Requirements

- Create a Internet Gateway (e.g. NAT) Server first using the `nat-gw` module

## TODOs

- Make control plane node schedueable if its a single node cluster
